<% layout("layouts/boilerPlate") %> <% const safeCoordinates = list.geometry &&
list.geometry.coordinates ? list.geometry.coordinates : [0, 0]; %>

<script>
  const mapToken = "<%= process.env.MAP_TOKEN %>";
  const coordinates = (<%- JSON.stringify(safeCoordinates) %>);
  const listingLocation = (<%- JSON.stringify(list.location) %>);
</script>

<div class="row mt-3">
  <div class="col-8 offset-3">
    <h3><b><%=list.title%></b></h3>
  </div>

  <div class="card col-6 offset-3 show-card listing-card">
    <img
      src="<%= list.image.url %>"
      class="card-img-top show-img"
      alt="list_image"
    /><br />
    <div class="card-body">
      <p class="card-text">
        <b>Owned By</b> : <b><%= list.owner?.username || 'Unknown' %></b>
      </p>
      <p class="card-text"><b>Description</b> : <%=list.description%></p>
      <p class="card-text">
        <b>Price</b> : &#8377; <%= list.price.toLocaleString("en-IN") %> /per
        night
      </p>
      <p class="card-text"><b>Location</b> : <%=list.location%></p>
      <p class="card-text"><b>Country</b> : <%=list.country%></p>
    </div>
  </div>

  <% if(currUser && list.owner && list.owner._id.equals(currUser._id)) {%>
  <div class="btns">
    <!-- this is to edit list  -->
    <a
      href="/listings/<%= list._id %>/edit"
      class="btn btn-dark edit-btn col-1 offset-3"
      >Edit</a
    >
    <!-- this is to delete list  -->
    <form method="POST" action="/listings/<%= list._id %>?_method=DELETE">
      <button class="btn btn-dark offset-6">Delete</button>
    </form>
  </div>
  <% } %>

  <!-- Reviews -->

  <div class="col-8 offset-3 mb-3">
    <hr />
    <% if(currUser){ %>
    <h4>Leave a Reviews</h4>
    <form
      action="/listings/<%= list._id %>/reviews"
      method="POST"
      novalidate
      class="needs-validation"
    >
      <div class="mb-3 mt-3">
        <label for="Rating" class="form-label">Rating</label>
        <fieldset class="starability-slot">
          <input
            type="radio"
            id="no-rate"
            class="input-no-rate"
            name="review[rating]"
            value="1"
            checked
            aria-label="No rating."
          />
          <input
            type="radio"
            id="first-rate1"
            name="review[rating]"
            value="1"
          />
          <label for="first-rate1" title="Terrible">1 star</label>
          <input
            type="radio"
            id="first-rate2"
            name="review[rating]"
            value="2"
          />
          <label for="first-rate2" title="Not good">2 stars</label>
          <input
            type="radio"
            id="first-rate3"
            name="review[rating]"
            value="3"
          />
          <label for="first-rate3" title="Average">3 stars</label>
          <input
            type="radio"
            id="first-rate4"
            name="review[rating]"
            value="4"
          />
          <label for="first-rate4" title="Very good">4 stars</label>
          <input
            type="radio"
            id="first-rate5"
            name="review[rating]"
            value="5"
          />
          <label for="first-rate5" title="Amazing">5 stars</label>
        </fieldset>
      </div>

      <div class="mb-3 mt-3">
        <label for="comments" class="form-label">Comment</label>
        <textarea
          name="review[comment]"
          id="comments"
          cols="30"
          rows="5"
          class="form-control"
          required
        ></textarea>
        <!-- client side validations -->
        <div class="invalid-feedback">
          Please write some comments for reviews...
        </div>
      </div>
      <button class="btn btn-outline-dark">Submit</button>
    </form>
    <hr />
    <%}%> <% if(list.reviews.length > 0){ %>
    <div class="row">
      <p><b>All Reviews</b></p>
      <% for (review of list.reviews) {%>
      <div class="card col-5 ms-3 mb-3">
        <div class="card-body">
          <h5 class="card-title pt-2">
            @<%= review.author?.username || 'Anonymous' %>
          </h5>
          <p
            class="starability-result card-text"
            data-rating="<%= review.rating %>"
          ></p>
          <p class="card-text"><%= review.comment %></p>

          <form
            class="mb-3"
            action="/listings/<%=list._id%>/reviews/<%=review._id%>?_method=DELETE"
            method="POST"
          >
            <button class="btn btn-dark btn-sm">Delete</button>
          </form>
        </div>
      </div>
      <%}%>
    </div>
    <% } %>
  </div>
  <div class="col-8 offset-3 mb-3">
    <h4>Where you'll be</h4>
    <div id="map" style="height: 300px"></div>
  </div>
</div>

<script>
  console.log("safeCoordinates:", <%= JSON.stringify(safeCoordinates) %>);
  console.log("list.geometry:", <%= JSON.stringify(list.geometry) %>);
</script>

<script src="/js/map.js"></script>
